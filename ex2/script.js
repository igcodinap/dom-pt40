// Using querySelector to select the first element that matches a CSS selector
const paragraph = document.querySelector(".text");
console.log(paragraph);
console.dir(paragraph);
paragraph.style.color = "blue";
